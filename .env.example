# The Odds API — required for live market spreads (US book median).
# Never commit a real key. The CLI never prints this value.
THE_ODDS_API_KEY=replace_me

# Optional fallback if THE_ODDS_API_KEY is unset.
# ODDS_API_KEY=replace_me
